PORT=3000
DB_HOST=db
# create a non-root user for the application; using "root" will cause the
# MySQL container to complain and restart repeatedly. Change this to any
# other username you like (e.g. statsuser).
DB_USER=statsuser
DB_PASSWORD=example
DB_NAME=stats
DB_PORT=3306
